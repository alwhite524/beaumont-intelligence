# SQL Query Library

These queries are intended for SQLite and the Beaumont Project Library schema.

Use named parameter `:keyword` in `03_documents_by_topic.sql`.

Examples:

```bash
sqlite3 database/archive.db < database/queries/01_stewart_park_meetings.sql
```
